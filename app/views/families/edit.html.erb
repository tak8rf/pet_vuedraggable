<%= form_with(model: @family, local: true) do |form| %>
  
  
    <div class="pet">
      <div class="form-tag">Family</div>
      <%= form.text_field :name %>
    </div>

  
    <div class="submit">
      <%= form.submit %>
    </div>
  
  <% end %>
